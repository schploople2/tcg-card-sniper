{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "corepack enable && rm -rf node_modules client/node_modules server/node_modules && NODE_ENV=development pnpm install --frozen-lockfile=false && pnpm prisma generate && pnpm --filter server build"
  },
  "deploy": {
    "startCommand": "pnpm prisma migrate deploy && pnpm --filter server start",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 60,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
